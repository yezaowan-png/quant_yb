# Codex 协作使用指南

这份文档记录“怎么和 Codex 高效协作”。它不是项目功能说明，而是协作手册：你可以用它理解我怎么读取规则、怎么执行任务、什么时候会改文件、怎样提需求最省来回、以及我们这个量化项目里必须固定遵守的验证习惯。

## 1. Codex 在这个项目里是什么

Codex 是一个运行在你本地工作区里的编程协作 agent。它可以：

- 阅读仓库文件、理解代码结构、梳理影响链路。
- 修改文件、补测试、更新文档。
- 在终端里运行命令，并把关键结果汇报给你。
- 用 Git 检查变更、提交、推送、更新 PR。
- 对本地 HTML 页面用 Playwright 做截图和视觉验证。
- 在需要时查 GitHub PR、issue、CI 状态。

Codex 不是单纯聊天机器人。默认情况下，如果你的请求明显是在让它完成一个工程任务，它会倾向于直接执行，而不是只给建议。你想让我先停在分析阶段时，需要明确说“先不要改代码”或“先出计划”。

## 2. `AGENTS.md` 是什么

`AGENTS.md` 是给 AI agent 看的项目级操作手册。它和 `README.md` 的区别是：

- `README.md` 面向人类用户，解释项目怎么用。
- `TECHNICAL.md` 面向开发者，解释架构和技术细节。
- `AGENTS.md` 面向 Codex 这类 agent，规定“在这个仓库里做事应该怎么做”。

适合写进 `AGENTS.md` 的内容：

- 项目定位和目录结构。
- 固定运行环境，例如本项目的 `E:\anaconda3\envs\QYTX\python.exe`。
- 必须使用的验证命令。
- 不能破坏的输出格式、CSV 字段、CLI 参数。
- 回测可信度规则，例如禁止未来函数、注意幸存者偏差、成交价格必须来自订单成交回调。
- Git 工作流，例如不能回滚用户未提交改动。
- 回复格式要求，例如中文、先结论、列修改文件和验证命令。

不适合写进 `AGENTS.md` 的内容：

- Tushare token、账号密码、私密路径和其他敏感信息。
- 一次性任务，例如“今天帮我改 dashboard”。
- 大规模运行产物、缓存数据、交易流水内容。

如果你发现我反复忘某个规则，最好的处理方式不是继续提醒我，而是把规则固化到 `AGENTS.md`，再加一个可执行检查。比如这次 QYTX 环境问题，我们已经加了 `scripts/verify_env.py`。

## 3. 其他项目文档怎么分工

建议按这个边界维护文档：

- `README.md`：普通用户怎么安装、下载数据、回测、生成报告。
- `TECHNICAL.md`：架构、模块链路、指标口径、数据流、回测模型细节。
- `AGENTS.md`：Codex 做任务时必须遵守的规矩。
- `CODEX_COLLABORATION_GUIDE.md`：你和 Codex 怎么沟通、怎么验收、怎么降低误解。
- 专题方案文档，例如 `QUANT_PROJECT_IMPROVEMENT_PLAN.md`：阶段计划、路线图、调研结论。

一个简单判断：如果这条信息是“项目怎么运行”，放 README；如果是“代码为什么这样设计”，放 TECHNICAL；如果是“Codex 必须怎么干活”，放 AGENTS；如果是“我该怎么向 Codex 下任务”，放本文件。

## 4. 我执行任务时的默认流程

除非你明确要求只讨论，我通常按这个流程推进：

1. 检查工作区状态，避免覆盖你的未提交改动。
2. 阅读相关入口、配置、策略、引擎、报告和文档，不只凭文件名猜。
3. 明确影响链路，例如 CLI 参数、配置、数据读取、策略、回测引擎、输出、报告。
4. 做小范围修改，避免顺手大重构。
5. 补必要测试和文档。
6. 用项目固定环境验证。
7. 如果是 UI 或 HTML 页面，用 Playwright 打开真实页面或代表性页面截图检查。
8. 汇报修改文件、验证命令、通过情况、未验证项和风险。
9. 如果任务包含保存或 PR 工作，提交并推送。

本项目里的 Python 项目验证必须使用：

```bash
E:\anaconda3\envs\QYTX\python.exe
```

每轮验证前先跑：

```bash
E:\anaconda3\envs\QYTX\python.exe scripts/verify_env.py
```

Playwright 浏览器截图可以继续使用系统已有的 `playwright` CLI，但生成 HTML、运行项目代码、单测和编译必须用 QYTX。

## 5. 怎样提需求最高效

最有效的请求通常包含六个要素：

```text
目标：我要达到什么结果
范围：允许改哪些模块，不想动哪些模块
模式：直接执行 / 先出计划 / 只分析不改
约束：环境、数据源、回测口径、兼容性、不能破坏的输出
验证：希望跑哪些命令，或者至少希望看到什么证明
交付：要代码、文档、PR、截图、报告，还是只要结论
```

例子：

```text
先不要改代码。帮我看 dashboard 阶段 4 还有哪些风险，重点看数据来源、未来函数边界和页面信息密度。最后给我一个分阶段修改计划。
```

```text
直接执行。把 dashboard 的策略排行榜改成支持按收益、夏普、回撤切换。不要改 CSV 字段。用 QYTX 跑测试，并用 Playwright 截图给我真实页面路径。
```

```text
做一次 code review。只看 PR 里的回测逻辑，优先找未来函数、成交价格、手续费和 T+1 问题。先列问题，不要直接改。
```

如果你只说“优化一下”，我会根据上下文做合理判断，但它容易导致范围变大。你可以加一句“先限定在 dashboard，不碰回测引擎”，效率会高很多。

## 6. 你可以明确选择的工作模式

### 直接执行

适合你已经知道要改什么时：

```text
直接执行，把 xxx 改成 yyy，跑测试并提交。
```

我会自己读代码、改文件、验证、总结。

### 先出计划

适合需求还比较大或不确定时：

```text
先出计划，不要改代码。
```

我会先给阶段、风险、影响范围和建议顺序。

### 只读分析

适合你想理解现状时：

```text
只分析，不改文件。帮我解释这个模块怎么跑。
```

我会只读仓库和命令输出。

### Code Review

只要你说“review”，我会默认进入代码审查模式：先列 bug、风险、回归、缺测试，不会把总结放在前面。

```text
review 这个 PR，重点看策略信号有没有未来函数。
```

### 结对调试

适合出现错误日志时：

```text
这是报错，帮我复现并修。先找最小复现，再改。
```

最好贴完整命令、错误栈、你期望的结果。

## 7. 给量化任务时最好补充的信息

量化项目里，缺信息很容易导致“看起来能跑，但结论不可信”。尽量补这些：

- 标的范围：单标的、多个股票、全市场、指数。
- 时间范围：例如 `20210101` 到 `20231231`。
- 数据来源：本地缓存、Tushare 下载、临时样例数据。
- 策略名和参数：例如 `sma_cross --fast 5 --slow 20`。
- 手续费、滑点、涨跌停、成交量限制是否要保持原口径。
- 输出要求：CSV、HTML 报告、dashboard、截图、PR。
- 可信度关注点：未来函数、幸存者偏差、基准缺失、成交约束、样本量不足。

好请求示例：

```text
用本地缓存验证 sma_cross 在 000001.SZ 上 2021-2023 的单标的报告。手续费、滑点用 config.yaml 原配置，不要改交易模型。生成 HTML 后用 Playwright 打开看图是否正常。
```

## 8. 给前端和报告页面任务时最好补充的信息

如果任务涉及 dashboard、HTML 报告、图表页面，最好说清楚：

- 页面目标用户：自己研究、给别人汇报、日常监控、策略复盘。
- 第一屏最重要的信息是什么。
- 是否要密集数据面板，还是偏展示型报告。
- 是否要支持移动端。
- 是否需要真实数据截图验证。
- 哪些链接或输出路径必须保持不变。

本项目报告页默认应该偏“研究工具”而不是营销页：信息密度高、入口清晰、指标口径明确、风险提示显眼。

## 9. 验证与截图怎么配合

代码验证和页面验证是两件事。

项目代码验证用 QYTX：

```bash
E:\anaconda3\envs\QYTX\python.exe scripts/verify_env.py
E:\anaconda3\envs\QYTX\python.exe -m unittest discover -s tests
E:\anaconda3\envs\QYTX\python.exe -m compileall main.py cli data engine strategy visual analysis decision scripts
```

页面查看用 Playwright CLI：

```bash
playwright screenshot --full-page --viewport-size "1440,1000" --wait-for-selector "text=QuantYB 研究总控" "file:///E:/quant_code/quant_yb/output/reports/dashboard.html" "E:\quant_code\quant_yb\output\reports\dashboard-real.png"
```

如果页面是用真实数据生成，我会在回复里说明：

- HTML 路径。
- 截图路径。
- 使用的是 QYTX 生成，还是临时样例数据生成。
- 是否依赖本地 `config.yaml`、`data/cache`、`output`。

## 10. Git 和 PR 怎么配合

如果你说“提交”“开 PR”“保存方便回退”，我会：

- 先 `git status`，确认有没有无关改动。
- 只暂存本次相关文件。
- 提交前说明范围。
- 推送到当前工作分支。
- 如果需要，创建或更新 PR。

如果仓库里有你自己的未提交文件，我不会回滚，也不会顺手加入提交。当前这个工作区里 `.agents/` 和 `.claude/settings.json` 就是未跟踪本地文件，除非你明确说要处理，我会继续避开它们。

## 11. 什么时候你应该打断我

你可以随时打断，尤其是这些情况：

- 我用了错误环境。
- 我理解错了任务范围。
- 我开始改你不想动的模块。
- 我跑了临时数据，但你要真实数据。
- 我只做了命令级验证，但你需要 Playwright 视觉验证。
- 我没有说明数据来源、时间范围或回测假设。

打断时最好直接说：

```text
停，先别继续。你现在用错环境了，必须切到 QYTX 重跑。
```

或者：

```text
这个不要用临时数据，必须用 E:\quant_code\quant_yb 里的真实 config 和 output。
```

这种指令越具体，我修正越快。

## 12. 推荐的常用提示词模板

### 实现功能

```text
直接执行。目标是【功能】。范围限定在【模块/文件】。不要改变【兼容性要求】。用 QYTX 跑环境哨兵、单测和编译；如果涉及页面，用 Playwright 截图。完成后提交到当前 PR。
```

### 先做方案

```text
先不要改代码。基于当前仓库和【参考文档/计划】，给我一个修改计划。计划要包含阶段、影响文件、验证方式和主要风险。
```

### 查问题

```text
只分析不改。帮我定位【现象/错误】的原因。请说明你读了哪些文件、判断链路是什么、最可能原因和下一步建议。
```

### 复盘量化结果

```text
基于本地缓存和当前 config，复盘【策略】【标的/范围】【日期范围】。说明数据源、手续费/滑点设置、输出路径和可信度限制。不要把未来收益用于策略信号。
```

### 页面验收

```text
真实运行生成页面，不要用临时数据。用 QYTX 生成 HTML，用 Playwright 截图。最后给我 HTML 路径、截图路径，以及你肉眼看到的主要问题。
```

## 13. 我这边的承诺

后续在这个项目里，我会把下面几件事当成默认协作契约：

- 项目 Python 命令先用 QYTX，不再用裸 `python`。
- 每轮验证前先跑 `scripts/verify_env.py`。
- 涉及页面就优先做 Playwright 截图验证。
- 真实数据和临时数据必须说清楚。
- 改回测逻辑必须说明影响成交价格、手续费、滑点、涨跌停、成交量限制、T+1、绩效指标和输出字段中的哪些部分。
- 对未来函数、数据泄露、幸存者偏差、基准缺失、缓存过期主动提示。
- 提交时只提交本次相关文件，不碰你的无关改动。

这份手册以后也可以继续迭代。每次我们踩到一个协作坑，就把它变成一条明确规则或一个可执行检查。
